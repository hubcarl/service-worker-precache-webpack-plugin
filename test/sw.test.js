'use strict';
const expect = require('chai').expect;
const path = require('path');
const fs = require('fs');
// http://chaijs.com/api/bdd/
describe('sw.test.js', () => {
  before(() => {
  });

  after(() => {
  });

  beforeEach(() => {
  });

  afterEach(() => {
  });

  describe('#webpack sw test', () => {
    it('should config html template', () => {
     
    });
  });
});